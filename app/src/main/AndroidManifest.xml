<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.threeabn.apps.mysdainterless">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:name=".MainActivity" android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/search" android:name=".SearchActivity"/>
        <activity android:label="@string/favorite" android:name=".FavoriteActivity"/>
        <activity android:label="@string/list" android:name=".ListActivity"/>
        <activity android:label="@string/channel" android:name=".MyChannelActivity"/>
    </application>

</manifest>